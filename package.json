{
  "name": "roost",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "cd apps/web && npx vite",
    "build": "cd apps/web && npx vite build",
    "api": "cd apps/api && node src/index.js"
  }
}
